namespace EnterKratos.StateMachines.States
{
    // ** Don't reorder as it will retarget already serialised values **
    public enum EnemyState
    {
        Idle,
        Patrol,
        Attack,
        Chase,
        Death
    }
}